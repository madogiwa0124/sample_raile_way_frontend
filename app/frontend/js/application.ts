// Entry point for the build script in your package.json
import { sampleHello } from "@js/sample";

const hello: string = "Hello from application.ts!";

console.log(hello);
console.log(sampleHello());
